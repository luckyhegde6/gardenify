# Session 2026-08-02 → 2026-08-03 — Remove SQLite Entirely → Supabase-Only Backend + OpenCV Best Practices

> **Archived from `.agents/session-todos.md`** — refactor complete. Commit hash: `21f82f7` (PR #21).

## User decisions (clarified before starting)

1. Remove SQLite **entirely** (delete `local_db.py`, `schema.sql`, `gardenify.db`; rewrite all importers to write to Supabase).
2. Research OpenCV image-identification best practices → **apply improvements to `image_processor.py` + document**.
3. `local_identify` must hit **Supabase** (works on Vercel + local Supabase), not SQLite.
4. Local dev uses **local Supabase** (`http://127.0.0.1:54321`) per `.env.local` — no SQLite fallback anywhere.

## Root cause recap (why this refactor)

- Vercel: `api/data/gardenify.db` is in `.vercelignore` so it never ships; `local_db.py` `sqlite3.connect()` fails → "Local identification failed: unable to open database file".
- Supabase `species` table has 10,008 rows but **no image hashes** (`get_hash_count()` returns 0).
- Local SQLite `gardenify.db` holds 10,008 species + **1,960 `image_hashes`** (phash+dhash, `{species_id}\img.jpg`, 1 hash per species id). Species ids are `BIGSERIAL` in Supabase vs autoincrement in SQLite → **must map by `scientific_name`, not id**.

## Refactor checklist (all done)

- [x] **Step 1 — Migration `008_image_hashes_table.sql`**: `image_hashes` table (species_id FK → species.id, image_path, phash, dhash, category, created_at) + indexes + RLS.
- [x] **Step 2 — Hash seed to Supabase (REGENERATE)**: `scripts/build_hash_index.py` → Supabase (`get_species_id_map()` + `insert_image_hash`). (Prod run deferred.)
- [x] **Step 3 — `supabase_species.py`**: added `find_by_phash()`, `insert_image_hash()`, `get_species_images()`, real `get_hash_count()`, `get_species_id_map()`.
- [x] **Step 4 — `local_identify.py`**: rewritten to call Supabase `find_by_phash` only.
- [x] **Step 5 — `identify.py`**: gate on `supabase_species.is_available()`.
- [x] **Step 6 — `species.py`**: dropped `_get_backend()` SQLite fallback → Supabase only.
- [x] **Step 7 — `main.py`**: removed `local_db.init_db()` + `seed_database()` startup block.
- [x] **Step 8 — Delete SQLite**: deleted `api/services/local_db.py`, `api/data/schema.sql`, `api/data/gardenify.db`; removed sqlite refs from `.vercelignore`.
- [x] **Step 9 — Rewrite importers to Supabase**: `seed_species.py`, `import_gbif.py`, `import_plantnet300k.py`, `build_hash_index.py`, `run_all.py`. Shared pattern: `seed_supabase_gbif.seed_supabase_gbif_from_list()` (batch upsert, `_to_list()` jsonb, merge-preserve).
- [x] **Step 10 — Rewrite tests**: `conftest.py` `FakeSupabaseClient` + `patched_supabase` fixture (no live Supabase); `test_species_routes.py`, `test_identify_offline.py`, `test_gbif_import.py`, `test_local_db.py` (→ Supabase data-layer tests, 21 tests).
- [x] **Step 11 — OpenCV research + apply**: `image_processor.py` now GaussianBlur→Canny, variance-of-Laplacian blur detection (`BLUR_THRESHOLD=100.0`), HSV green-pixel ratio; `OpenCVResult` gained `sharpness`/`is_blurry`/`green_ratio`; 4 new tests.
- [x] **Step 12 — Docs**: LESSONS.md, AGENTS.md/MEMORY.md/BUGS.md/handoff updated.
- [x] **Step 13 — Verify**: `npx tsc --noEmit` clean + `cd api && ruff check .` clean + `cd api && pytest` **91 passed**. Backend live-verified + emulator E2E (Monstera 81.7%).

## Environment facts

- Local Supabase: `http://127.0.0.1:54321` (currently DOWN — run `npx supabase start`, or `USE_REMOTE=true` for prod).
- Prod Supabase: project `amyriuhwqyalodsfkwzf`. Management API token lives in gitignored `C:\Users\lucky\AppData\Local\Temp\opencode\creds.json`.
- `.env.local`: has both local keys (top) AND prod keys (bottom, remote block) — `SUPABASE_SERVICE_ROLE_KEY` line 8 is LOCAL, line 37 is PROD.

## Deferred to later sessions

- Migrate prod hashes: apply migration 008 + run hash seed against prod Supabase (~20-30 min).
- Verify on Vercel: POST valid image → 200 with `source: "local"` when phash matches; no "Local identification failed" warning.
