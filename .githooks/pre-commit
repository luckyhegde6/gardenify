#!/bin/sh

branch=$(git symbolic-ref --short HEAD 2>/dev/null)

case "$branch" in
  main|master)
    echo "error: direct commits to '$branch' are not allowed (PR-only workflow)." >&2
    echo "Create a branch first, e.g.:  git checkout -b feat/your-change" >&2
    exit 1
    ;;
esac

exit 0
